{
  "name": "is-equal-enough",
  "version": "3.0.2",
  "description": "Determine whether two numbers are within a provided tolerance of each other",
  "repository": "flipactual/is-equal-enough",
  "author": {
    "name": "Flip",
    "id": 1306968,
    "url": "https://github.com/flipactual"
  },
  "license": "MIT",
  "main": "index.js",
  "engines": {
    "node": ">=6.0.0"
  },
  "scripts": {
    "test": "nyc mocha",
    "lint": "eslint ./**/*.js --",
    "coverage": "nyc report --reporter=html mocha && open ./coverage/index.html",
    "codecov": "nyc report --reporter=text-lcov > coverage.lcov && codecov",
    "readme": "emdaer"
  },
  "devDependencies": {
    "codecov": "^1.0.1",
    "emdaer": "^0.3.2",
    "eslint": "^3.2.2",
    "eslint-config-airbnb-base": "^5.0.1",
    "eslint-plugin-import": "^1.13.0",
    "mocha": "^2.3.4",
    "nyc": "^9.0.1"
  }
}
